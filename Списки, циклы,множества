#Есть 2 списка. Нужно удалить из 1 списка повторяющиеся значения 2 списка
a = [2, 5, 8, 2, 12, 12, 4, 5, 8, 12, 3, 6, 19]
b = [2, 7, 12, 3,83, 8, 14]

result = set(a) - set(b)  #приводим списки к множеству сет и вычитаем 1 из другого
print (list(result)) #В результате все повторяющиеся значения пропали из 1 списка- неправильно

for number in a:
    if number in b:
        a.remove(number)
        print(a) # тоже не правильно, так как если значения стоят подряд, то он их пропускает

# ПРАВИЛЬНО Сделать копию списка a (срез от начала до конца)
for number in a[:]:
    if number in b:
        a.remove(number)
        print(a)
